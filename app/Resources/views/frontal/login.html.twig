{#plantilla de nuesta aplicacion#}
{% extends 'base.html.twig' %}

 {% block title %} 
Login
 {% endblock %}
{#contenido#}
 {% block body %}

<div class="container">

    <!-- Intro Content -->
    <div class="row">
      <div class="col-lg-6">
        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        <form action="{{ path('login') }}" method="post">
         <div class="control-group form-group">
            <div class="controls">
                <label for="username">Username:</label>
                <input class ="form-control" type="text" id="username" name="_username" value="{{ last_username }}" />
            </div>
        </div>
        <div class="control-group form-group">
            <div class="controls">
                <label for="password">Password:</label>
                <input class = "form-control" type="password" id="password" name="_password" />
            </div>
        </div>

    {#
        If you want to control the URL the user
        is redirected to on success (more details below)
        <input type="hidden" name="_target_path" value="/account" />
    #}

    <button  class="btn btn-primary" type="submit">login</button>
</form>
      </div>
      <div class="col-lg-6">
        <h2>¿Por qué registrarse?</h2>
        <p>con el registro usted puede realizar reservas para nuestro restaurant</p>
        <p>mediante correo electronico y contraseña</p>
        <p>podra realizar una nueva reserva o anulación de esta</p>
        <p>si no esta registrado por favor pinche en </p>
        <a href="{{path ('registro') }}">Registro</a>
      </div>
    </div>
    <!-- /.row -->
</div>
 {% endblock %}